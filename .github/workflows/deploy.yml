name: Deploy to GitHub Pages

on:
  push:
    branches: [master]      # any push to master → rebuild and publish
  workflow_dispatch:        # + can also be triggered manually from the Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2          # the project has bun.lock → build with bun
      - run: bun install --frozen-lockfile
      - run: bun run build                   # = tsc --noEmit && vite build → dist/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist                         # the build output folder

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
